Profiles:

    GamblARGenesis:        
        Orderer:
            <<: *OrdererDefaults
            Organizations:
                - *OrdererOrg   
        Application:
            <<: *ApplicationDefaults
            Organizations:
                - *Org1
                - *Org2
        Consortium: SampleConsortium         
        Consortiums:
            SampleConsortium:
                Organizations:
                    - *Org1
                    - *Org2
    indianchannel:
        Consortium: SampleConsortium
        Application:
            <<: *ApplicationDefaults
            Organizations:
                - *Org1
                - *Org2
            


Organizations:
    - &OrdererOrg
        Name: OrdererOrg
        ID: gamblar.com
        AdminPrincipal: Role.ADMIN
        MSPDir: crypto-config/ordererOrganizations/gamblar.com/msp

    - &Org1
        Name: Org1MSP
        ID: org1.gamblar.com
        AdminPrincipal: Role.ADMIN
        MSPDir: crypto-config/peerOrganizations/org1.gamblar.com/msp
        AnchorPeers:
            - Host: peer0.org1.gamblar.com
              Port: 7051

    - &Org2
        Name: Org2MSP
        ID: org2.gamblar.com
        MSPDir: crypto-config/peerOrganizations/org2.gamblar.com/msp
        AnchorPeers:
            - Host: peer0.Org2.gamblar.com
              Port: 9051


Orderer: &OrdererDefaults

    # Orderer Type: The orderer implementation to start
    # Available types are "solo" and "kafka"
    OrdererType: solo

    Addresses:
        - orderer.gamblar.com:7050

    # Batch Timeout: The amount of time to wait before creating a batch
    BatchTimeout: 2s

    # Batch Size: Controls the number of messages batched into a block
    BatchSize:

        # Max Message Count: The maximum number of messages to permit in a batch
        MaxMessageCount: 10

        # Absolute Max Bytes: The absolute maximum number of bytes allowed for
        # the serialized messages in a batch.
        AbsoluteMaxBytes: 99 MB

        # Preferred Max Bytes: The preferred maximum number of bytes allowed for
        # the serialized messages in a batch. A message larger than the preferred
        # max bytes will result in a batch larger than preferred max bytes.
        PreferredMaxBytes: 512 KB

    Kafka:
        # Brokers: A list of Kafka brokers to which the orderer connects
        # NOTE: Use IP:port notation
        Brokers:
            - 127.0.0.1:9092
    Organizations:



Application: &ApplicationDefaults
    Organizations: